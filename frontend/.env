# URL de l'API backend
# Laisser à /api — nginx proxifie /api/ vers le container "backend:8000" en interne Docker
# NE PAS mettre l'IP du serveur (ex: http://217.76.59.131:8000) — ce serait une requête directe du navigateur
VITE_API_URL=/api
